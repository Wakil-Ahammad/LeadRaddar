# ═══════════════════════════════════════════════════
# LeadRadar — Environment Variables
# Copy this file to .env and fill in your values
# ═══════════════════════════════════════════════════

# ── Server ──
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=dev

# ── Database (PostgreSQL) ──
DB_HOST=localhost
DB_PORT=5434
DB_NAME=serviceintent_radar
DB_USER=radar
DB_PASSWORD=radar_dev_2026

# ── Redis ──
REDIS_HOST=localhost
REDIS_PORT=6380

# ── JWT ──
JWT_SECRET=change-me-to-a-secure-random-256-bit-key-in-production!!
JWT_EXPIRATION_MS=86400000

# ── Reddit API (https://www.reddit.com/prefs/apps) ──
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=

# ── OpenAI API (https://platform.openai.com/api-keys) ──
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# ── Stripe (https://dashboard.stripe.com/test/apikeys) ──
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# ── Email (SMTP) ──
MAIL_HOST=localhost
MAIL_PORT=1025

# ── Collector Schedule (cron expression) ──
COLLECTOR_CRON=0 0 0 * * *
